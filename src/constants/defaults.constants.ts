import { Units } from './units.constants';

export const DEFAULT_CITY = 'Sofia';
export const DEFAULT_UNITS = Units.Metric;

export const HOURLY_FORECAST_DEFAULT_LENGTH = 25;
export const DAILY_FORECAST_DEFAULT_LENGTH = 8;
